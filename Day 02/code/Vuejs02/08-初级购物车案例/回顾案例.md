
#### 首先，创建三个文件夹，分别对应html，js，css文件
#### 观察原型图，搭建对应的表格
#### 采用table进行搭建
#### cv对应的css样式
#### 搭建js的格式，将books的数据以数组的形式引入到data中
#### 使用v-for循环遍历条数
#### 用item.属性名获取对应值
#### 思考”价格”前的人民币符号和后面的小数点二位如何加入
#### 采取使用filter过滤器的方式
#### 在js中option添加filter过滤器，传入对应的price价格，然后返回一个封装好符号和小数点后二位的值
#### 在对应价格的td中，以 price | 过滤器(price) 的形式使用过滤器
#### 购买价格，左右各设置一个button，分别对应增加和删除
#### 在js中option添加methods，新增添加和删除的方法，因为需要知道是哪一条数据的数量增加或减少，因此需要考虑拿到索引值（index)
#### 修改v-for遍历的时候新增获取对应的index值
#### 完善对应的新增和删除的方法，在对应的button标签中，以@click=方法名(index)的形式，实现+1和-1的操作
#### 但是考虑到，数量不可为负数,为负数时按钮不显示
#### 在button标签中绑定事件  使用v-bind:disabled，通过判断item.count是否小于等于1，得到一个布尔值，来判断button按钮是否显示，实现这个效果
#### 操作中提到 移除按钮 同理是需要对应的索引（index），在button绑定单击事件，使用splice()函数实现
#### 考虑到如果全部内容都被移除，则需要展示：购物车为空的字样，将整个div用v-if来判断他的books.length是否大于0，否则展示”购物车为空“
#### 计算总价格，使用js中option的computed，用for循环遍历累加价格和数量的乘积，得到最后的总价格，使用过滤器进行样式补全。